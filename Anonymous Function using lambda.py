# taking the input from user
n = int(input("Enter the value here = "))
# Defining a function using lambda 
f = lambda a : a *a   # Anonymous Functions are called as lambda expressions

result = f(n) # here we are finding square of n 

print (result)
